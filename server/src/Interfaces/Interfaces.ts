export interface authData {
  name: string;
  email: string;
  password: string;
}

export interface userData {
  firstName: string;
  lastName: string;
  phoneNo: string | number;
  address: string;
  email: string;
}
